<%= turbo_stream.replace(@section, partial: "quotes/section", locals: { section: @section }) %>

<%= turbo_stream.replace("quote-total", partial: "quotes/quote_total", locals: { quote: @section.quote }) %>
