<%#= turbo_frame_tag "new_section" do %>
<%= simple_form_for [@quote, Section.new],
    html: { class: "d-flex gap-2 align-items-start" },
    data: { turbo_stream: true, action: "submit->sections#submitForm" } do |f| %>

  <%= f.input :description,
      label: false,
      placeholder: "Nom de la nouvelle section",
      wrapper_html: { class: "flex-grow-1" },
      input_html: { class: "form-control form-control-sm" } %>

  <%= f.button :submit,
        "+ Créer la section",
        class: "btn btn-primary btn-sm" %>

  <button type="button" class="btn btn-sm btn-outline-secondary" data-action="click->sections#hideNewSectionForm">
    Annuler
  </button>
<% end %>
<%# end %>
