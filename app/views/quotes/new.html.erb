<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm rounded-3">
        <div class="card-body">
          <h2 class="card-title mb-4 text-primary">
            <i class="bi bi-file-earmark-plus me-2"></i>Commençons votre nouveau devis
          </h2>
          <p class="text-muted mb-4">
            Quelques informations clés pour commencer à chiffrer votre projet.
          </p>

          <%= form_for [@company, @quote], local: true do |form| %>
            <div class="mb-4">
              <%= form.label :title, "Titre du projet", class: "form-label fw-semibold" %>
              <%= form.text_field :title, class: "form-control", placeholder: "Ex : Rénovation complète appartement T3", required: true %>
            </div>

            <div class="mb-4">
              <div class="d-flex justify-content-between align-items-center mb-2">
                <%= form.label :client_id, "Client", class: "form-label fw-semibold mb-0" %>
                <%= link_to new_company_client_path(@company), class: "btn btn-sm btn-outline-primary" do %>
                  <i class="bi bi-person-plus"></i> Créer un client
                <% end %>
              </div>
              <%= form.collection_select :client_id, Client.all, :id, :first_name, { prompt: "Sélectionnez un client" }, { class: "form-control", required: true } %>
            </div>

            <div class="d-flex justify-content-between mt-4">
              <%= link_to "← Retour", company_quotes_path, class: "btn btn-outline-secondary" %>
              <%= form.submit "Démarrer le devis", class: "btn btn-primary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
